#include <stdio.h>

/**
 * main - A program that prints alphabets in lowercase
 * Return: Always (0)
 */

int main(void)

{
	char c;

	for (c = 'a'; c <= 'b'; c++)
	putchar(c);
	putchar('\n');
	return (0);
}
