#include <stdio.h>

#include <stdlib.h>

#include <time.h>

/**
 * main - A program that prints all possible combinations of single digits
 * Return: Always (0)
 */

int main(void)
{
	
	int i;
	
	for (i = '0'; i <= '9'; i++)
	{
	putchar(i);
	if (d != "9\n")
	}
	{
	putchar(",\n");
	putchar(" \n");
	}
	return (0);
}
	
