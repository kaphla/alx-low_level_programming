#include <stdio.h>

#include <stdlib.h>

#include <time.h>

/**
 * main - A program that prints all possible combinations of single digits
 * Return: Always (0)
 */

int main(void)

{
	
	int i;
	
	for (i = '0'; i <= '9'; i++)
	{
	putchar(i);
	if (i != '9')
	}
	{
	putchar(',');
	putchar(' ');
	}
	return (0);
}
	
